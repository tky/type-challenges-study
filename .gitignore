node_modules/
.DS_Store
.idea
dist
*.log
*.local
# playground/ is tracked on purpose: it holds my answers.
# .playgroundcache stays ignored — its hashes embed absolute file paths,
# so it is machine-specific and regenerated by `pnpm generate`.
.playgroundcache